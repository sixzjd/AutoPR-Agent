github:
  token: "${GITHUB_TOKEN}"
  webhook_secret: "${WEBHOOK_SECRET}"

anthropic:
  api_key: "${ANTHROPIC_API_KEY}"
  models:
    spec_agent: "claude-sonnet-4-20250514"
    change_agent: "claude-opus-4-20250514"
    test_agent: "claude-sonnet-4-20250514"
    arbiter: "claude-opus-4-20250514"

agents:
  timeout_seconds: 30
  max_retries: 2

review:
  blocking_labels: ["BLOCKER", "CRITICAL"]
  auto_merge_block: true
  cache_ttl_hours: 24
